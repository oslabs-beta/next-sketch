import { Tabs, Tab, Typography, Box } from '@mui/material';

interface tabPanelProps {
  children?: React.ReactNode;
  index: number;
  value: number;
}

const Tabs = ({ chidlren, value, index }: tabPanelProps) => {
  return( 
  <div>
    

  </div>
  );
};

export default Tabs;
